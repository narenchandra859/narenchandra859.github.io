Drop watch photos in this folder (e.g. `watches/omega-seamaster.jpg`), then
update the `.watch-card` items in `index.html` to use `<img>` tags pointing
here, same pattern as the `travel_imgs` gallery.
